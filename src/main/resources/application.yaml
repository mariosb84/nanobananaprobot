# Basic Application Settings
spring:
  application:
    name: nanobananaprobot
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/nanobananaprobot_auth}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:SB14041984sb}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: false
  # Настройка асинхронных задач для @Async
  task:
    execution:
      pool:
        core-size: 5
        max-size: 20
        queue-capacity: 100

  # Logging
  logging:
    level:
      root: warn
      org:
        springframework:
          web: error
          security: DEBUG
          # Включим логи для ваших сервисов
      org.example.nanobananaprobot: DEBUG

server:
  port: 9900

# Telegram Bot
tg:
  username: ${TELEGRAM_BOT_USERNAME:NanoBananaProBot_bot}
  token: ${TELEGRAM_BOT_TOKEN:8527977714:AAFu97URfKY8nc5uwoKlATtGp7xE1V_y6X4}

# WebDriver
webdrivermanager:
  cache:
    path: "./drivers"
  download-proxy: "DIRECT"

# YooKassa Payment System
yookassa:
  shop-id: ${YOOKASSA_SHOP_ID:1180332}
  secret-key: ${YOOKASSA_SECRET_KEY:live_TwRr2APY3ngv0M-aBnhAewcpSk8oPyltkLGRg_Pgh-g}
  test-mode: false

# ProxyAPI для DALL-E 3
# ВНИМАНИЕ: Никогда не храните ключи прямо в YAML!
# Используйте переменные окружения: export PROXYAPI_KEY="ваш_ключ"
proxyapi:
  key: ${PROXYAPI_KEY:sk-R4KbljvvsNuEXYdjqQ4lCBYwTgxVeTin}  # Безопасное хранение ключа

# Application Settings
app:
  payment:
    return-url: "https://t.me/NanoBananaProBot_bot"
  webhook:
    url: "http://localhost:9900/api/payments/webhook/yookassa"
  packages:
    image:
      "3": "39"
      "10": "99"
      "50": "449"
      "100": "799"
      "300": "2099"
    video:
      "1": "50"
      "5": "225"
      "10": "399"
  trial:
    free-generations: "3"
    period-days: "7"
  subscription:
    monthly:
      price: "299.00"
    yearly:
      price: "2490.00"

# URLs
paymentUrl: "https://yoomoney.ru/checkout/payments/v2/contract?orderId="

# Prices and Currency
subscriptionPlanYearly: "2490.00"
subscriptionPlanMonthly: "299.00"
amountMonthly: "299.00"
amountYearly: "2490.00"
amountSetCurrency: "RUB"
currency: " ₽"
currencySecond: "₽"

# API URLs для YooKassa
API_URL: "https://api.yookassa.ru/v3/"
TEST_API_URL: "https://api.yookassa.ru/v3/"

# RestTemplate
rest:
  connection:
    timeout: 5000
  read:
    timeout: 5000

# Настройки для нового ProxyApiImageService
image:
  generation:
    timeout: 30000  # Таймаут для HTTP-клиента
    model: "dall-e-3"  # Используемая модель
    size: "1024x1024"  # Размер изображения
    quality: "standard"  # Качество

higgsfield:
  client-id: ${HIGGSFIELD_CLIENT_ID:a990c48e-9642-4714-aabf-5513b4e51394}
  client-secret: ${HIGGSFIELD_CLIENT_SECRET:4805a6812bc969f4cc09469ec8939b97933634c1ed0fefac09e87451ebce3603}
cometapi:
  url: "https://api.cometapi.com/v1beta/models/gemini-3-pro-image-preview:generateContent"
  key: ${COMET_API_KEY:sk-MwuE5tWfsaX20dORAPTopKzjvyJRYyccqYEVMK9VUCznbJ9W}